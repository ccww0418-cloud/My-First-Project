{
  "name": "bookbot-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:saved": "node scripts/saved-test.mjs",
    "check:render": "vite build --ssr scripts/render-check.jsx --outDir .render --emptyOutDir --logLevel error && node .render/render-check.js"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.3.4",
    "vite": "6.0.7"
  }
}
